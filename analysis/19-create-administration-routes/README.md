# Analysis: create administration routes

This folder stores the delegated APEX analysis handoff.

- `prompts/`: one prompt per exploration agent.
- `reports/`: one report per completed agent.

Agents must only report existing code, docs, contracts, gaps, and line references. They must not implement, plan, or modify project files.
