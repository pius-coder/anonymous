# v0.1 — RPC mount matrix (SEQ-03)

**Date:** 2026-07-16  
**Branch:** `integration/v0.1-completion`  
**Freeze source:** `packages/contracts/src/matrix.ts` (SEQ-01)

## Freeze totals

| Metric | Value | Notes |
|--------|------:|-------|
| Frozen services | **12** | Was 11 in pre-SEQ-01 gap analysis |
| Frozen methods | **57** | Was 50 in pre-SEQ-01 gap analysis |
| Historical AC wording | 11 / 50 | Superseded by SEQ-01 freeze; keep 12 / 57 |

## Central router (`apps/api/src/rpc/routes.ts`)

After SEQ-03 composition:

| # | Service | Handler export | Mounted | Methods impl / freeze |
|---|---------|----------------|:-------:|----------------------:|
| 1 | IdentityService | `identityService` | yes | 8 / 8 |
| 2 | SessionService | `sessionService` | yes | 4 / 4 |
| 3 | ParticipationService | `participationService` | yes | 3 / 3 |
| 4 | PreparationService | `preparationService` | yes | 5 / 5 |
| 5 | PaymentService | `paymentService` | yes | 4 / 4 |
| 6 | RoundService | `roundService` | yes | 10 / 10 |
| 7 | RealtimeAccessService | `realtimeService` | yes | **1 / 4** |
| 8 | ScoringService | `scoringService` | yes | 4 / 4 |
| 9 | MiniGameService | — | **no** | 0 / 2 |
| 10 | AdminService | — | **no** | 0 / 3 |
| 11 | NotificationService | — | **no** | 0 / 4 |
| 12 | ComplianceService | — | **no** | 0 / 6 |

**Mounted services:** 8 / 12  
**Mounted method implementations present:** 39 / 57 freeze methods (Realtime missing 3 Connect methods; 4 services without handlers)

## Remaining gaps (documented, not SEQ-03 blockers)

1. **RealtimeAccessService** — only `CreateLiveAccess` on Connect; snapshots live in Colyseus (A-REALTIME game-server). Client facades still call `GetPlayerState` / admin / readonly Connect methods → will fail until handlers or transport strategy evolve.
2. **MiniGameService / AdminService / ComplianceService** — no API adapters yet (WAVE-B / later sprints).
3. **NotificationService** — A-WORKERS delivers via BullMQ + gateway; no Connect NotificationService adapter.
4. **Web** — `rpcClients` already defined for all 11 historical client surfaces; server mount was the missing piece for WAVE-A domains.

## Ownership notes

- SEQ-03 only mounts public exports; domain logic remains in lot files.
- A-WORKERS ownership review: **PASS** (worker + whatsapp-gateway + lockfile only; no schema/contracts/api prep/web/game-server).
- A-REALTIME integrator checkbox: L5 multi-service live smoke is `apps/web/e2e/live-smoke.spec.ts` after this composition.

## Proof

- Unit composition: `apps/api/src/rpc/__tests__/routes.composition.test.ts`
- Integration smokes: `pnpm test:integration`
- Multi-service live: `pnpm test:e2e` → `live-smoke.spec.ts`
