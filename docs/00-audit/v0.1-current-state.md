# Etat courant v0.1 — analyse du depot

Date d'observation : 2026-07-16
Branche : `v0.1`
Repertoire : `/home/afreeserv/anonymous`

Ce document decrit l'etat **reel** du code v0.1, distinct de l'audit HEAD legacy
(`current-system.md`, `current-routes-and-flows.md`, `feature-implementation-matrix.md`).

Documents complementaires :

- [Matrice d'implementation v0.1](v0.1-implementation-matrix.md)
- [Routes et flux v0.1](v0.1-routes-and-flows.md)
- [Analyse d'ecart sprints/couches](v0.1-gap-analysis.md)
- [Inventaire technique](repository-inventory.md)

## Resume executif

Le depot `session-jeu` est un monorepo **pnpm + Turbo** en reconstruction progressive.
La fondation v0.1 n'est pas un MVP jetable : elle reconstitue une plateforme multijoueur ambitieuse
(auth, paiements, live, admin, scoring, mini-jeux) avec des frontieres de couche explicites.

**Position actuelle** : socle technique solide, UI large en avance sur le branchement backend.

| Dimension | Maturite | Commentaire |
|---|---|---|
| Documentation produit/archi | Elevee | `docs/00-audit` a `06-roadmap` comme source de verite |
| Domaine pur (`game-engine`) | Elevee | State machines party/participation/round/score, spatial room |
| Persistence (Prisma) | Elevee | 21 modeles, 8 migrations, 11 repositories |
| Contrats Protobuf | Elevee | 13 fichiers `.proto`, generation buf, fixtures golden |
| API REST (Hono) | Moyenne-haute | Auth, parties, participation, preparation, paiements, rounds, live |
| ConnectRPC | Faible | 3 services sur 11 definis en proto |
| Game server (Colyseus) | Moyenne | Room sociale autoritaire, mouvement, reconnexion, snapshots |
| Web (Next.js) | UI haute / data faible | 40 pages, shells par acteur ; donnees mock sauf auth (+ live partiel) |
| Mini-jeux | Absent | Catalogue produit (120 titres) sans runtime |
| Workers / notifications | Stub | Logique de jobs testee, pas de consommateur BullMQ |
| Tests | Moyenne | ~44 fichiers ; domaine/API solides, UI structurelle, E2E debut |

## Validations executees (2026-07-16)

```text
pnpm typecheck  → 13/13 tasks OK
pnpm lint       → 13/13 tasks OK (avertissements codegen uniquement)
pnpm test       → 13/13 tasks OK (API 56 tests, web 15 tests, + autres workspaces)
pnpm build      → 9/9 tasks OK
pnpm docs:check → OK
pnpm --filter @session-jeu/web test:e2e → 2/2 tests OK
```

## Architecture monorepo

```text
apps/
  web/              Next.js 16 — UI joueur, admin, support, finance, observer
  api/              Hono 4 + ConnectRPC — commandes/queries applicatives
  game-server/      Colyseus 0.17 — etat live autoritaire
  worker/           Fonctions de jobs (sans runner BullMQ)
  whatsapp-gateway/ Placeholder provider notification

packages/
  contracts/        Protobuf → TypeScript (buf)
  db/               Prisma 6 + repositories
  game-engine/      Domaine pur, transitions, spatial
  shared/           Tokens opaques, erreurs transversales
```

Infra locale : `docker-compose.yml` (PostgreSQL 16 + Redis 7).

## Decisions architecturales respectees

Confirmes dans le code et la doc :

- Timer et jobs ne demarrent jamais une partie active sans commande admin.
- Scores provisoires en DB ; pas d'API de publication encore.
- Parcours admin / joueur / observer separes au niveau routes et layouts.
- Serveur autoritaire pour positions, rounds en room, deadlines.
- Contrats Protobuf comme cible reseau (REST encore dominant cote API).

## Ecarts majeurs vs cible documentaire

1. **UI non cablee** — La plupart des pages utilisent `ui-data.ts`, `finance-data.ts`,
   `observer-data.ts`, `support-data.ts` au lieu des endpoints REST existants.
2. **ConnectRPC incomplet** — Seuls `IdentityService`, `RoundService`, `RealtimeAccessService`
   sont enregistres dans `apps/api/src/rpc/routes.ts`.
3. **Scoring** — Modeles Prisma + transitions domaine ; zero route API/RPC.
4. **Mini-jeux** — Zero runtime ; room Colyseus = social + orchestration round minimale.
5. **BullMQ** — Dependance declaree, aucun Worker/Queue en runtime.
6. **Seed** — `packages/db/prisma/seed.ts` vide ; setup manuel requis.
7. **Port game-server** — `.env.example` mentionne 2567 ; serveur configure sur 3002 par defaut.

## Avancement roadmap (sprints 00–19)

| Sprint | Theme | Statut observe |
|---|---|---|
| 00 | Socle hygiene | Fait (docs, turbo, workspaces) |
| 01 | Modele domaine | Fait (game-engine transitions) |
| 02 | Contrats Protobuf | Fait (proto + gen + tests) |
| 03 | Persistence | Fait (schema + migrations + repos) |
| 04 | Auth RBAC | Partiel (REST + Identity RPC ; reset password RPC absent) |
| 05 | Acquisition publique | Partiel (API `listPublicParties` ; UI mock) |
| 06 | Participation | Fait (REST use-cases + tests) |
| 07 | Paiements wallet | Fait (REST ; webhook Fapshi ; reconciliation admin) |
| 08 | Preparation lobby | Fait (REST admin + joueur) |
| 09 | Realtime core | Partiel (GameRoom, reconnect, snapshots ; pas de minigame state) |
| 10 | Round orchestration | Partiel (REST + Round RPC ; close deadline via worker function) |
| 11 | Experience joueur live | Partiel (Phaser room + pages ; fallback offline preview) |
| 12 | Admin command center | Partiel (UI riche ; donnees hardcodees) |
| 13 | Scoring publication | Non commence (API) |
| 14–15 | Mini-jeux | Non commence |
| 16 | Observer | Partiel (UI mock ; snapshots serveur existent) |
| 17 | Notifications workers | Stub |
| 18 | Compliance audit | Partiel (AuditLog en API ; UI compliance shell) |
| 19 | Migration legacy | En cours (branche v0.1 active) |

## Lot de finalisation

Le lot finalise est concentre sur :

- **game-server** : movement handler, command dispatcher, GameRoom, LiveRoomState
- **web** : sprint UI (layouts, parcours parties, Phaser room, admin/support/finance)
- **contracts** : evenements realtime proto
- **game-engine** : module spatial
- **docs** : frontend-architecture, component inventory, target-architecture

Fichiers notables ajoutes : `realtime-service.ts`, `movement-handler.ts`, e2e Playwright,
`interface-coverage.test.ts`, composants admin/observer/finance/player.

## Risques immediats

| Risque | Impact | Mitigation documentee |
|---|---|---|
| Drift UI/API | Recette impossible sans mock | Sprint UI gate + branchement `rpcServices` |
| RPC partiel | Clients web appellent des services non implementes | Prioriser services par parcours critique |
| Pas de seed | Dev/demo lents | Sprint 03 — seed minimal parties/users |
| Tests UI structurels | Faux positif couverture | Enrichir tests comportement + E2E |
| GAME_WS_URL / port | Connexion live echoue en dev | Aligner `.env.example` et config game-server |

## Prochaines actions recommandees (ordre)

1. Brancher catalogue et detail partie (`GET /v1/parties`) sur `apps/web`.
2. Completer `SessionService`, `ParticipationService`, `PreparationService` en RPC ou documenter exception REST.
3. Implementer `ScoringService` (sprint 13) avant exposition joueur des resultats.
4. Activer runner BullMQ pour `closeDueRoundDeadlines` et reconciliation paiements.
5. Seed minimal + E2E parcours inscription → lobby → room.

## Lecture obligatoire associee

- `docs/README.md`
- `docs/03-architecture/target-architecture.md`
- `docs/06-roadmap/sprint-plan.md`
- `docs/06-roadmap/sprint-UI/README.md`
- `docs/00-audit/head-forensic-audit.md` (legacy, pour reprise selective)
