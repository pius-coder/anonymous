# Apex Workflow

Dans ce depot, Apex est traite comme workflow interne deja present (`.codex/output/apex/*`, `AGENTS.md`).

Pour une future implementation:

- branche non-main;
- lecture docs locales;
- Context7 pour librairies;
- implementation par feature;
- tests typecheck/lint/test/build;
- PR seulement quand demande et workflow autorise.

Pour cette mission v0.1:

- pas de PR;
- pas de push;
- pas de code metier;
- documentation et manifeste uniquement.

